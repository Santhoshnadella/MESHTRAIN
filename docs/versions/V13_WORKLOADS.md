# VERSION 13 — LARGE-SCALE MIXED WORKLOAD MESH

Support:

    inference
    fine-tuning
    federated learning
    batch inference
    embedding generation
    evaluation
    model benchmarking
    dataset processing
    research experiments

Workloads become scheduler plugins.

Example:

    workload type
         ↓
    requirements
         ↓
    scheduler
         ↓
    topology
         ↓
    peer selection
